# ============================================================
# requirements-pipeline.txt
# Dependencies for the GitHub Actions pipeline runner ONLY.
# NOT used by Vercel (which has its own requirements.txt).
# ============================================================

# HTTP
httpx==0.27.0
requests==2.31.0

# RSS parsing
feedparser==6.0.11

# AI SDKs
google-generativeai==0.8.3
groq==0.9.0

# HuggingFace — NEW Inference Providers SDK (replaces raw httpx image calls)
# AsyncInferenceClient.text_to_image() is the correct 2026 API for FLUX etc.
huggingface_hub>=0.28.0

# Image processing — required to convert PIL.Image → PNG bytes
Pillow>=10.0.0

# Utilities
python-dateutil==2.9.0
pytz==2024.1
tenacity==8.3.0
python-dotenv==1.0.1
