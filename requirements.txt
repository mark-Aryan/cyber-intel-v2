# ============================================================
# requirements.txt — Vercel serverless API dependencies
#
# FIX (2026): Removed fastapi, mangum, httpx.
# Vercel's @vercel/python runtime requires BaseHTTPRequestHandler.
# FastAPI + Mangum causes: TypeError: issubclass() arg 1 must be a class
#
# The new api/index.py uses stdlib only (urllib, http.server, json)
# so NO extra packages are needed at all.
# ============================================================
